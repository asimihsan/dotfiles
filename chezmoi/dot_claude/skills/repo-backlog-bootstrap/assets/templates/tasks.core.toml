[tasks.fmt]
description = "Run formatting checks"
run = [
  "echo 'TODO: replace with project formatter command'",
]

[tasks.lint]
description = "Run lint checks"
run = [
  "echo 'TODO: replace with project lint command'",
]

[tasks.test]
description = "Run tests"
alias = ["t"]
run = [
  "echo 'TODO: replace with project test command'",
]

[tasks.dev]
description = "Default local quality loop"
run = [
  "mise run fmt",
  "mise run lint",
  "mise run test",
]

[tasks.doctor]
description = "Print core mise diagnostics"
run = [
  "mise doctor",
  "mise tasks",
]
