[project]
name = "fetch-skill"
version = "0.1.0"
description = "Python fetch helper for Claude skill workflows"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27,<1",
  "markdownify>=0.13,<1",
  "readabilipy>=0.3,<1",
]

[tool.uv]
package = false
